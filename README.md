# boolean-ir-retail-search
 A basic Information Retrieval system built using Boolean search over a retail dataset. Supports logical queries (AND, OR, NOT) with stemming and precision/recall evaluation
